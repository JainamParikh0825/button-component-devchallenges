import React from 'react';
import { StyledContainer } from '../assets/StyledContainer';

const Spaces = () => {
    return (
        <StyledContainer>
            <h2>Spaces</h2>
        </StyledContainer>
    );
};

export default Spaces;